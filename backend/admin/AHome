<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Home</title>
    <link rel="icon" href="https://via.placeholder.com/32" type="image/png">
    
    <style>
        :root {
            --primary-color: #4a6bff;
            --secondary-color: #2c3e50;
            --background-color: #f5f5f5;
            --text-color: #333;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--background-color);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
        }

        .top-bar {
            position: absolute;
            top: 20px;
            right: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .add-btn {
            background-color: var(--primary-color);
            color: var(--white);
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .add-btn:hover {
            background-color: #3755cc;
        }

        .profile-container {
            position: relative;
            display: flex;
            align-items: center;
            cursor: pointer;
        }

        .profile-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--secondary-color);
        }

        .dropdown {
            position: absolute;
            top: 50px;
            right: 0;
            background: var(--white);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
            width: 150px;
            display: none;
            flex-direction: column;
        }

        .dropdown a {
            padding: 10px;
            text-decoration: none;
            color: var(--text-color);
            display: block;
            transition: background 0.3s ease;
        }

        .dropdown a:hover {
            background: var(--primary-color);
            color: var(--white);
        }

        .profile-container:hover .dropdown {
            display: flex;
        }

        .container {
            max-width: 600px;
        }

        h1 {
            font-size: 2rem;
            color: var(--secondary-color);
            margin-bottom: 1.5rem;
        }

        p {
            font-size: 1.1rem;
            color: var(--text-color);
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>

    <!-- Top Bar with Add Location Button & Profile -->
    <div class="top-bar">
        <div class="profile-container">
            <a href="AProfile">
                <div class="profile-button">
                    <img src="asset/user.png" alt="Profile" class="profile-icon">
                </div>
            </a>
        </div> 
    </div>

   
    

</body>
</html>
